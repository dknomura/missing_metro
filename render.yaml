services:
  - type: web
    name: sb79-map
    runtime: python
    buildCommand: pip install -e .
    startCommand: marimo run notebooks/sb79map.py --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.13